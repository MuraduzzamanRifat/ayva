<!-- AYVA Hero Banner: Two images side-by-side with centered SHOP NOW -->
{%- style -%}
  .ayva-hero {
    position: relative;
    width: 100%;
    overflow: hidden;
  }
  .ayva-hero__grid {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 0;
    min-height: 85vh;
  }
  .ayva-hero__grid--single {
    grid-template-columns: 1fr;
  }
  @media screen and (max-width: 749px) {
    .ayva-hero__grid {
      grid-template-columns: 1fr;
      min-height: auto;
    }
  }
  .ayva-hero__image {
    width: 100%;
    height: 100%;
    overflow: hidden;
  }
  .ayva-hero__image img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    display: block;
  }
  .ayva-hero__cta {
    position: absolute;
    top: 50%;
    left: 50%;
    transform: translate(-50%, -50%);
    z-index: 10;
    background: rgba(255, 255, 255, 0.95);
    padding: 2.4rem 4rem;
    text-align: center;
  }
  @media screen and (max-width: 749px) {
    .ayva-hero__cta {
      position: relative;
      top: auto;
      left: auto;
      transform: none;
      padding: 2rem 3rem;
      margin: -2rem auto 0;
      width: 80%;
      z-index: 10;
    }
  }
  .ayva-hero__btn {
    display: inline-block;
    font-family: var(--ayva-font-body);
    font-size: 1.1rem;
    font-weight: 700;
    text-transform: uppercase;
    letter-spacing: 0.18em;
    color: #000;
    background: transparent;
    border: 2px solid #000;
    padding: 1.2rem 3.6rem;
    text-decoration: none;
    transition: all 0.3s ease;
  }
  .ayva-hero__btn:hover {
    background: #000;
    color: #fff;
  }
{%- endstyle -%}

<section class="ayva-hero">
  <div class="ayva-hero__grid{% if section.settings.image_right == blank %} ayva-hero__grid--single{% endif %}">
    <div class="ayva-hero__image">
      <img
        src="{{ section.settings.image_left | default: '' }}"
        alt="AYVA bags lifestyle"
        loading="eager"
        width="2000"
        height="1300"
      >
    </div>
    {%- if section.settings.image_right != blank -%}
    <div class="ayva-hero__image">
      <img
        src="{{ section.settings.image_right }}"
        alt="AYVA bags lifestyle"
        loading="eager"
        width="1000"
        height="1200"
      >
    </div>
    {%- endif -%}
  </div>
  <div class="ayva-hero__cta">
    <a href="{{ section.settings.button_link | default: '/collections/all' }}" class="ayva-hero__btn">
      {{ section.settings.button_text | default: 'SHOP NOW' }}
    </a>
  </div>
</section>

{% schema %}
{
  "name": "AYVA Hero Banner",
  "settings": [
    {
      "type": "text",
      "id": "image_left",
      "label": "Left image URL",
      "default": ""
    },
    {
      "type": "text",
      "id": "image_right",
      "label": "Right image URL",
      "default": ""
    },
    {
      "type": "text",
      "id": "button_text",
      "label": "Button text",
      "default": "SHOP NOW"
    },
    {
      "type": "url",
      "id": "button_link",
      "label": "Button link"
    }
  ],
  "presets": [
    {
      "name": "AYVA Hero Banner"
    }
  ]
}
{% endschema %}
